<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
html: {method: :delete}, remote: true) do |f| %>
    <button class="button button--primary button--withChrome is-active" style="width: 120px; margin-bottom: 10px;">
      Following
    </button>
<% end %>
